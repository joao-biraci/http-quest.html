<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <meta name="description" content="HTTP Quest: O Guardião da Web - Jogo educativo épico sobre protocolos HTTP/HTTPS">
    <meta name="theme-color" content="#1e3c72">
    <title>🌐 HTTP QUEST: A Lenda do Guardião da Web</title>

    <style>
        @import url('https://fonts.googleapis.com/css2?family=Press+Start+2P&family=Roboto:wght@400;700;900&family=Fira+Code:wght@400;600&display=swap');

        :root {
            --primary: #1e3c72;
            --secondary: #2a5298;
            --accent: #00ff88;
            --danger: #ff4444;
            --warning: #FFD700;
            --magic: #ff00ff;
            --cyber: #00ffff;
            --dark: #0a0a0a;
            --glass: rgba(0, 0, 0, 0.9);
            --font-pixel: 'Press Start 2P', cursive;
            --font-main: 'Roboto', sans-serif;
            --font-code: 'Fira Code', monospace;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            -webkit-tap-highlight-color: transparent;
        }

        body {
            font-family: var(--font-main);
            background: linear-gradient(135deg, #0f0c29 0%, #302b63 50%, #24243e 100%);
            overflow: hidden;
            height: 100vh;
            touch-action: none;
            color: white;
        }

        @keyframes float {
            0%, 100% { transform: translateY(0) rotate(0deg); }
            50% { transform: translateY(-20px) rotate(5deg); }
        }

        @keyframes glitch {
            0% { transform: translate(0); }
            20% { transform: translate(-2px, 2px); }
            40% { transform: translate(-2px, -2px); }
            60% { transform: translate(2px, 2px); }
            80% { transform: translate(2px, -2px); }
            100% { transform: translate(0); }
        }

        @keyframes pulse-glow {
            0%, 100% { box-shadow: 0 0 20px var(--accent); }
            50% { box-shadow: 0 0 40px var(--accent), 0 0 60px var(--cyber); }
        }

        @keyframes rainbow {
            0% { filter: hue-rotate(0deg); }
            100% { filter: hue-rotate(360deg); }
        }

        @keyframes shake {
            0%, 100% { transform: translateX(0); }
            25% { transform: translateX(-10px) rotate(-5deg); }
            75% { transform: translateX(10px) rotate(5deg); }
        }

        @keyframes bounce {
            0%, 100% { transform: scale(1); }
            50% { transform: scale(1.1); }
        }

        @keyframes slideIn {
            from { transform: translateX(-100%); opacity: 0; }
            to { transform: translateX(0); opacity: 1; }
        }

        @keyframes typewriter {
            from { width: 0; }
            to { width: 100%; }
        }

        @keyframes scanline {
            0% { transform: translateY(-100%); }
            100% { transform: translateY(100vh); }
        }

        @keyframes flicker {
            0%, 100% { opacity: 1; }
            50% { opacity: 0.8; }
            70% { opacity: 0.95; }
            90% { opacity: 0.85; }
        }

        @keyframes pixel-bounce {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-8px); }
        }

        @keyframes neon-pulse {
            0%, 100% { text-shadow: 0 0 5px var(--accent), 0 0 10px var(--accent), 0 0 20px var(--accent); }
            50% { text-shadow: 0 0 10px var(--accent), 0 0 20px var(--accent), 0 0 40px var(--cyber); }
        }

        @keyframes border-glow {
            0%, 100% { border-color: var(--accent); box-shadow: 0 0 10px var(--accent); }
            50% { border-color: var(--cyber); box-shadow: 0 0 20px var(--cyber), 0 0 40px var(--accent); }
        }

        @keyframes rotate-lock {
            0%, 100% { transform: rotate(0deg); }
            25% { transform: rotate(-5deg); }
            75% { transform: rotate(5deg); }
        }

        #gameContainer {
            width: 100vw;
            height: 100vh;
            position: relative;
            display: flex;
            flex-direction: column;
        }

        /* ===== SPLASH SCREEN / LOGIN ===== */
        #splashScreen {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(180deg, #0a0a1a 0%, #1a0a2e 50%, #0d1b2a 100%);
            z-index: 3000;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            overflow: hidden;
        }

        #splashScreen::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: 
                linear-gradient(rgba(0,255,136,0.03) 1px, transparent 1px),
                linear-gradient(90deg, rgba(0,255,136,0.03) 1px, transparent 1px);
            background-size: 40px 40px;
            pointer-events: none;
        }

        .scanline-overlay {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 4px;
            background: rgba(0,255,136,0.3);
            animation: scanline 4s linear infinite;
            pointer-events: none;
            z-index: 10;
        }

        .splash-logo {
            font-family: var(--font-pixel);
            font-size: 32px;
            color: var(--accent);
            text-shadow: 
                4px 4px 0 #000,
                0 0 20px var(--accent),
                0 0 40px var(--cyber);
            margin-bottom: 20px;
            animation: neon-pulse 2s infinite, flicker 3s infinite;
            text-align: center;
            line-height: 1.5;
        }

        .splash-subtitle {
            font-family: var(--font-pixel);
            font-size: 12px;
            color: var(--cyber);
            margin-bottom: 40px;
            text-shadow: 2px 2px 0 #000;
            animation: pixel-bounce 2s infinite;
        }

        /* Pixel Art Lock Icon */
        .pixel-lock-container {
            width: 120px;
            height: 120px;
            margin: 20px auto;
            position: relative;
            animation: rotate-lock 3s ease-in-out infinite;
        }

        .pixel-lock {
            width: 100%;
            height: 100%;
            position: relative;
        }

        .pixel-lock-shackle {
            width: 40px;
            height: 40px;
            border: 8px solid #FFD700;
            border-bottom: none;
            border-radius: 20px 20px 0 0;
            position: absolute;
            top: 0;
            left: 50%;
            transform: translateX(-50%);
            box-shadow: 0 0 15px rgba(255,215,0,0.5);
        }

        .pixel-lock-body {
            width: 80px;
            height: 60px;
            background: linear-gradient(135deg, #FFD700, #FFA500);
            border: 4px solid #CC8400;
            position: absolute;
            bottom: 0;
            left: 50%;
            transform: translateX(-50%);
            border-radius: 4px;
            box-shadow: 0 0 20px rgba(255,215,0,0.4);
        }

        .pixel-lock-keyhole {
            width: 16px;
            height: 20px;
            background: #0a0a0a;
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            border-radius: 8px 8px 2px 2px;
        }

        .pixel-lock-keyhole::after {
            content: '';
            width: 8px;
            height: 12px;
            background: #0a0a0a;
            position: absolute;
            bottom: -8px;
            left: 50%;
            transform: translateX(-50%);
        }

        .splash-buttons {
            display: flex;
            flex-direction: column;
            gap: 15px;
            width: 90%;
            max-width: 400px;
            margin-top: 20px;
        }

        /* Pixel Art Buttons - Estilo placa de madeira/cyberpunk */
        .pixel-btn {
            background: linear-gradient(180deg, #4a5568 0%, #2d3748 100%);
            border: 4px solid #718096;
            border-bottom: 8px solid #1a202c;
            border-radius: 4px;
            color: white;
            font-family: var(--font-pixel);
            font-size: 14px;
            padding: 20px 30px;
            cursor: pointer;
            text-transform: uppercase;
            position: relative;
            transition: all 0.1s;
            text-shadow: 2px 2px 0 #000;
            box-shadow: 
                inset 0 2px 0 rgba(255,255,255,0.1),
                0 4px 0 #1a202c,
                0 8px 20px rgba(0,0,0,0.5);
        }

        .pixel-btn:hover {
            background: linear-gradient(180deg, #5a6578 0%, #3d4758 100%);
            transform: translateY(-2px);
            box-shadow: 
                inset 0 2px 0 rgba(255,255,255,0.2),
                0 6px 0 #1a202c,
                0 10px 25px rgba(0,255,136,0.3);
        }

        .pixel-btn:active {
            transform: translateY(4px);
            border-bottom: 4px solid #1a202c;
            box-shadow: 
                inset 0 2px 0 rgba(0,0,0,0.2),
                0 0 0 #1a202c,
                0 2px 10px rgba(0,0,0,0.3);
        }

        .pixel-btn-start {
            background: linear-gradient(180deg, #48bb78 0%, #2f855a 100%);
            border-color: #68d391;
            border-bottom-color: #22543d;
            color: #fff;
            box-shadow: 
                inset 0 2px 0 rgba(255,255,255,0.2),
                0 4px 0 #22543d,
                0 8px 20px rgba(0,255,136,0.3);
            animation: pulse-glow 2s infinite;
        }

        .pixel-btn-start:hover {
            background: linear-gradient(180deg, #56c78a 0%, #38a169 100%);
            box-shadow: 
                inset 0 2px 0 rgba(255,255,255,0.3),
                0 6px 0 #22543d,
                0 10px 30px rgba(0,255,136,0.5);
        }

        .pixel-btn-start:active {
            border-bottom: 4px solid #22543d;
        }

        .pixel-btn-danger {
            background: linear-gradient(180deg, #f56565 0%, #c53030 100%);
            border-color: #fc8181;
            border-bottom-color: #9b2c2c;
            box-shadow: 
                inset 0 2px 0 rgba(255,255,255,0.2),
                0 4px 0 #9b2c2c,
                0 8px 20px rgba(255,0,0,0.3);
        }

        .pixel-btn-danger:hover {
            background: linear-gradient(180deg, #fc8181 0%, #e53e3e 100%);
        }

        .pixel-btn-warning {
            background: linear-gradient(180deg, #ecc94b 0%, #b7791f 100%);
            border-color: #f6e05e;
            border-bottom-color: #975a16;
            color: #1a202c;
            text-shadow: none;
            box-shadow: 
                inset 0 2px 0 rgba(255,255,255,0.3),
                0 4px 0 #975a16,
                0 8px 20px rgba(255,215,0,0.3);
        }

        .pixel-btn-warning:hover {
            background: linear-gradient(180deg, #f6e05e 0%, #d69e2e 100%);
        }

        .pixel-decoration {
            font-size: 24px;
            margin: 0 10px;
            display: inline-block;
            animation: pixel-bounce 1s infinite;
        }

        /* ===== HUD ===== */
        #hud {
            height: 80px;
            background: linear-gradient(90deg, rgba(0,0,0,0.95), rgba(30,30,60,0.95));
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 0 25px;
            border-bottom: 4px solid var(--accent);
            box-shadow: 0 0 30px rgba(0,255,136,0.3);
            z-index: 100;
            font-family: var(--font-pixel);
            font-size: 10px;
        }

        .hud-section {
            display: flex;
            align-items: center;
            gap: 15px;
        }

        .stat-box {
            background: rgba(0,255,136,0.1);
            border: 3px solid var(--accent);
            border-radius: 8px;
            padding: 8px 15px;
            display: flex;
            align-items: center;
            gap: 10px;
            box-shadow: inset 0 0 10px rgba(0,255,136,0.1);
        }

        .stat-icon {
            font-size: 20px;
            animation: pixel-bounce 1s infinite;
        }

        .hp-bar, .xp-bar {
            width: 120px;
            height: 20px;
            background: #1a1a2e;
            border: 3px solid #4a5568;
            border-radius: 4px;
            overflow: hidden;
            position: relative;
            box-shadow: inset 0 2px 4px rgba(0,0,0,0.5);
        }

        .hp-fill {
            height: 100%;
            background: linear-gradient(90deg, #ff4444, #ff8844);
            transition: width 0.5s ease;
            box-shadow: inset 0 0 10px rgba(255,255,255,0.2);
        }

        .xp-fill {
            height: 100%;
            background: linear-gradient(90deg, var(--accent), var(--cyber));
            transition: width 0.5s ease;
            box-shadow: inset 0 0 10px rgba(255,255,255,0.2);
        }

        .bar-text {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            font-size: 9px;
            text-shadow: 2px 2px 0 #000;
            color: white;
        }

        .level-badge {
            background: linear-gradient(135deg, var(--warning), #ff6600);
            color: black;
            padding: 12px 18px;
            border-radius: 4px;
            font-weight: 900;
            border: 3px solid #b7791f;
            box-shadow: 
                inset 0 2px 0 rgba(255,255,255,0.3),
                0 4px 0 #975a16,
                0 0 20px rgba(255,215,0,0.5);
            animation: bounce 2s infinite;
            font-family: var(--font-pixel);
            font-size: 12px;
        }

        /* ===== GAME WORLD ===== */
        #gameWorld {
            flex: 1;
            position: relative;
            overflow: hidden;
            background: 
                linear-gradient(180deg, rgba(135,206,235,0.3) 0%, rgba(224,246,255,0.2) 50%, rgba(144,238,144,0.3) 50%, rgba(34,139,34,0.4) 100%);
        }

        .cyber-grid {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-image: 
                linear-gradient(rgba(0,255,136,0.03) 1px, transparent 1px),
                linear-gradient(90deg, rgba(0,255,136,0.03) 1px, transparent 1px);
            background-size: 50px 50px;
            pointer-events: none;
        }

        /* ===== CHARACTERS ===== */
        .character {
            position: absolute;
            cursor: pointer;
            transition: all 0.3s cubic-bezier(0.68, -0.55, 0.265, 1.55);
            z-index: 50;
            filter: drop-shadow(0 10px 20px rgba(0,0,0,0.5));
        }

        .character:hover {
            transform: scale(1.2) translateY(-10px);
            filter: drop-shadow(0 15px 30px rgba(0,255,136,0.4));
        }

        .character-avatar {
            width: 80px;
            height: 80px;
            border-radius: 8px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 40px;
            border: 4px solid white;
            box-shadow: 
                0 0 30px rgba(0,0,0,0.5),
                inset 0 0 20px rgba(255,255,255,0.1);
            animation: float 3s ease-in-out infinite;
            position: relative;
            overflow: hidden;
        }

        .character-avatar::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 4px;
            background: rgba(255,255,255,0.3);
        }

        .client-char {
            bottom: 20%;
            left: 8%;
        }

        .client-char .character-avatar {
            background: linear-gradient(135deg, #4285F4, #34A853);
            animation-delay: 0s;
            border-color: #34A853;
        }

        .server-char {
            bottom: 20%;
            right: 8%;
        }

        .server-char .character-avatar {
            background: linear-gradient(135deg, #EA4335, #FBBC05);
            animation-delay: 1s;
            border-color: #FBBC05;
        }

        .hacker-char {
            bottom: 35%;
            left: 45%;
            opacity: 0;
            transition: opacity 0.5s;
        }

        .hacker-char.visible {
            opacity: 1;
            animation: hackerFloat 4s ease-in-out infinite;
        }

        .hacker-char .character-avatar {
            background: linear-gradient(135deg, #000000, #434343);
            border-color: var(--danger);
            box-shadow: 0 0 40px rgba(255,0,0,0.6);
            animation: glitch 0.3s infinite;
        }

        @keyframes hackerFloat {
            0%, 100% { transform: translate(0, 0) rotate(0deg); }
            25% { transform: translate(-20px, -30px) rotate(-10deg); }
            50% { transform: translate(20px, -15px) rotate(10deg); }
            75% { transform: translate(-10px, -25px) rotate(-5deg); }
        }

        .character-name {
            position: absolute;
            bottom: -35px;
            left: 50%;
            transform: translateX(-50%);
            background: var(--dark);
            color: white;
            padding: 6px 14px;
            border-radius: 4px;
            font-family: var(--font-pixel);
            font-size: 8px;
            white-space: nowrap;
            border: 3px solid var(--accent);
            opacity: 0;
            transition: opacity 0.3s;
            box-shadow: 0 4px 0 rgba(0,0,0,0.5);
        }

        .character:hover .character-name {
            opacity: 1;
        }

        .dialogue-box {
            position: absolute;
            background: white;
            color: black;
            padding: 15px 20px;
            border-radius: 8px;
            border: 4px solid #333;
            font-family: var(--font-main);
            font-size: 14px;
            font-weight: 700;
            max-width: 250px;
            opacity: 0;
            transform: scale(0);
            transition: all 0.4s cubic-bezier(0.68, -0.55, 0.265, 1.55);
            z-index: 100;
            box-shadow: 5px 5px 0 rgba(0,0,0,0.3);
        }

        .dialogue-box.active {
            opacity: 1;
            transform: scale(1);
        }

        .dialogue-box::after {
            content: '';
            position: absolute;
            bottom: -15px;
            left: 30px;
            border: 15px solid transparent;
            border-top-color: #333;
        }

        .dialogue-box::before {
            content: '';
            position: absolute;
            bottom: -10px;
            left: 30px;
            border: 15px solid transparent;
            border-top-color: white;
            z-index: 1;
        }

        .data-packet {
            position: absolute;
            width: 50px;
            height: 50px;
            border-radius: 8px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 24px;
            z-index: 40;
            transition: all 0.1s linear;
            box-shadow: 0 5px 20px rgba(0,0,0,0.4);
            border: 3px solid;
        }

        .packet-normal {
            background: linear-gradient(135deg, #FFD700, #FFA500);
            border-color: #FF8C00;
        }

        .packet-secure {
            background: linear-gradient(135deg, #00CED1, #20B2AA);
            border-color: #008B8B;
            animation: pulse-glow 1s infinite;
        }

        .packet-hacked {
            background: linear-gradient(135deg, #ff4444, #cc0000);
            border-color: #990000;
            animation: shake 0.5s infinite;
        }

        /* ===== MODALS ===== */
        .modal {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0,0,0,0.95);
            z-index: 1000;
            justify-content: center;
            align-items: center;
            backdrop-filter: blur(10px);
        }

        .modal.active {
            display: flex;
            animation: modalFadeIn 0.4s ease;
        }

        @keyframes modalFadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }

        .modal-content {
            background: linear-gradient(135deg, #1a1a2e 0%, #16213e 100%);
            border: 4px solid var(--accent);
            border-radius: 8px;
            padding: 30px;
            max-width: 900px;
            width: 90%;
            max-height: 90vh;
            overflow-y: auto;
            position: relative;
            box-shadow: 
                0 0 60px rgba(0,255,136,0.3),
                inset 0 0 40px rgba(0,255,136,0.05);
            animation: modalSlideIn 0.5s cubic-bezier(0.68, -0.55, 0.265, 1.55);
        }

        @keyframes modalSlideIn {
            from { transform: translateY(-100px) scale(0.8); opacity: 0; }
            to { transform: translateY(0) scale(1); opacity: 1; }
        }

        .modal-header {
            text-align: center;
            margin-bottom: 25px;
            border-bottom: 3px solid rgba(0,255,136,0.3);
            padding-bottom: 20px;
        }

        .modal-title {
            font-family: var(--font-pixel);
            font-size: 20px;
            color: var(--warning);
            text-shadow: 3px 3px 0 #000;
            margin-bottom: 10px;
            animation: rainbow 3s linear infinite;
        }

        .modal-subtitle {
            color: #aaa;
            font-size: 16px;
        }

        .story-container {
            background: rgba(0,0,0,0.5);
            border: 3px solid var(--cyber);
            border-radius: 8px;
            padding: 20px;
            margin: 20px 0;
            font-size: 16px;
            line-height: 1.8;
            position: relative;
            box-shadow: inset 0 0 20px rgba(0,255,255,0.1);
        }

        .story-text {
            color: #fff;
            text-shadow: 0 0 10px rgba(0,255,255,0.5);
        }

        .typing-effect {
            overflow: hidden;
            white-space: nowrap;
            animation: typewriter 3s steps(40) 1s 1 normal both;
        }

        .challenge-container {
            background: linear-gradient(135deg, rgba(102,126,234,0.2), rgba(118,75,162,0.2));
            border: 4px solid var(--magic);
            border-radius: 8px;
            padding: 25px;
            margin: 20px 0;
            position: relative;
            overflow: hidden;
            box-shadow: inset 0 0 30px rgba(255,0,255,0.1);
        }

        .challenge-container::before {
            content: '🎯';
            position: absolute;
            top: -10px;
            right: -10px;
            font-size: 40px;
            opacity: 0.3;
            animation: float 2s ease-in-out infinite;
        }

        .challenge-title {
            font-family: var(--font-pixel);
            font-size: 14px;
            color: var(--magic);
            margin-bottom: 15px;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .minigame-area {
            background: rgba(0,0,0,0.6);
            border-radius: 8px;
            padding: 20px;
            margin: 15px 0;
            min-height: 200px;
            border: 2px solid rgba(255,255,255,0.1);
        }

        .memory-grid {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 15px;
            max-width: 400px;
            margin: 0 auto;
        }

        .memory-card {
            aspect-ratio: 1;
            background: linear-gradient(135deg, #667eea, #764ba2);
            border: 4px solid white;
            border-radius: 8px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 30px;
            cursor: pointer;
            transition: all 0.3s;
            transform-style: preserve-3d;
            box-shadow: 
                0 4px 0 #4a5568,
                0 8px 20px rgba(0,0,0,0.3);
        }

        .memory-card:hover {
            transform: scale(1.05) translateY(-4px);
            box-shadow: 
                0 8px 0 #4a5568,
                0 12px 30px rgba(102,126,234,0.6);
        }

        .memory-card.flipped {
            background: white;
            transform: rotateY(180deg);
        }

        .memory-card.matched {
            background: var(--accent);
            animation: bounce 0.5s;
            pointer-events: none;
            border-color: var(--accent);
            box-shadow: 0 0 20px rgba(0,255,136,0.5);
        }

        .memory-card.error {
            background: var(--danger);
            animation: shake 0.5s;
            border-color: var(--danger);
        }

        .typing-challenge {
            font-family: var(--font-code);
            font-size: 24px;
            background: #0a0a0a;
            padding: 20px;
            border-radius: 8px;
            border: 3px solid var(--accent);
            margin: 15px 0;
            position: relative;
            box-shadow: inset 0 0 20px rgba(0,255,136,0.1);
        }

        .typing-target {
            color: #666;
        }

        .typing-input {
            width: 100%;
            background: transparent;
            border: none;
            color: var(--accent);
            font-family: var(--font-code);
            font-size: 24px;
            outline: none;
            margin-top: 10px;
            text-shadow: 0 0 10px var(--accent);
        }

        .typing-correct {
            color: var(--accent);
            text-shadow: 0 0 10px var(--accent);
        }

        .typing-wrong {
            color: var(--danger);
            text-decoration: line-through;
        }

        .drag-container {
            display: flex;
            gap: 10px;
            flex-wrap: wrap;
            justify-content: center;
            margin: 20px 0;
            padding: 15px;
            background: rgba(0,0,0,0.3);
            border-radius: 8px;
            border: 2px dashed rgba(255,255,255,0.2);
        }

        .drag-item {
            background: linear-gradient(180deg, #4a5568 0%, #2d3748 100%);
            border: 3px solid var(--cyber);
            border-bottom: 6px solid #1a202c;
            border-radius: 8px;
            padding: 12px 18px;
            cursor: grab;
            font-family: var(--font-pixel);
            font-size: 10px;
            transition: all 0.2s;
            user-select: none;
            color: white;
            box-shadow: 0 4px 0 #1a202c;
            text-shadow: 1px 1px 0 #000;
        }

        .drag-item:hover {
            transform: translateY(-4px);
            box-shadow: 0 8px 0 #1a202c, 0 0 20px rgba(0,255,255,0.3);
            border-color: var(--accent);
        }

        .drag-item.dragging {
            opacity: 0.5;
            cursor: grabbing;
        }

        .drag-item.used {
            opacity: 0.3;
            pointer-events: none;
            border-color: #666;
            box-shadow: none;
            transform: translateY(4px);
        }

        .drop-zone {
            min-height: 100px;
            background: rgba(0,255,136,0.1);
            border: 3px dashed var(--accent);
            border-radius: 8px;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 10px 0;
            transition: all 0.3s;
            flex-wrap: wrap;
            gap: 10px;
            padding: 15px;
            font-family: var(--font-pixel);
            font-size: 14px;
        }

        .drop-zone.drag-over {
            background: rgba(0,255,136,0.3);
            border-style: solid;
            box-shadow: 0 0 30px rgba(0,255,136,0.3);
        }

        .drop-zone .drag-item {
            cursor: pointer;
        }

        /* ===== BUTTONS ===== */
        .btn {
            background: linear-gradient(180deg, #667eea 0%, #4a5568 100%);
            color: white;
            border: none;
            padding: 18px 35px;
            font-family: var(--font-pixel);
            font-size: 12px;
            cursor: pointer;
            border-radius: 8px;
            margin: 10px;
            transition: all 0.2s;
            box-shadow: 
                0 6px 0 #2d3748,
                0 10px 20px rgba(0,0,0,0.3);
            position: relative;
            text-transform: uppercase;
            border: 3px solid #718096;
            text-shadow: 2px 2px 0 #000;
        }

        .btn:active {
            transform: translateY(6px);
            box-shadow: 0 0 0 #2d3748, 0 4px 10px rgba(0,0,0,0.3);
        }

        .btn:hover {
            filter: brightness(1.2);
            transform: translateY(-2px);
            box-shadow: 
                0 8px 0 #2d3748,
                0 12px 25px rgba(0,0,0,0.4);
        }

        .btn-success {
            background: linear-gradient(180deg, #48bb78 0%, #2f855a 100%);
            border-color: #68d391;
            box-shadow: 
                0 6px 0 #22543d,
                0 10px 20px rgba(0,0,0,0.3);
            color: white;
        }

        .btn-success:active {
            box-shadow: 0 0 0 #22543d, 0 4px 10px rgba(0,0,0,0.3);
        }

        .btn-danger {
            background: linear-gradient(180deg, #f56565 0%, #c53030 100%);
            border-color: #fc8181;
            box-shadow: 
                0 6px 0 #9b2c2c,
                0 10px 20px rgba(0,0,0,0.3);
        }

        .btn-danger:active {
            box-shadow: 0 0 0 #9b2c2c, 0 4px 10px rgba(0,0,0,0.3);
        }

        .btn-warning {
            background: linear-gradient(180deg, #ecc94b 0%, #b7791f 100%);
            color: black;
            border-color: #f6e05e;
            box-shadow: 
                0 6px 0 #975a16,
                0 10px 20px rgba(0,0,0,0.3);
            text-shadow: none;
        }

        .btn-warning:active {
            box-shadow: 0 0 0 #975a16, 0 4px 10px rgba(0,0,0,0.3);
        }

        /* ===== REWARD POPUP ===== */
        .reward-popup {
            position: fixed;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%) scale(0);
            background: linear-gradient(135deg, #FFD700, #FFA500);
            color: black;
            padding: 40px;
            border-radius: 8px;
            text-align: center;
            z-index: 5000;
            box-shadow: 
                0 0 100px rgba(255,215,0,0.8),
                0 10px 0 #b7791f;
            transition: transform 0.5s cubic-bezier(0.68, -0.55, 0.265, 1.55);
            border: 4px solid #fff;
        }

        .reward-popup.show {
            transform: translate(-50%, -50%) scale(1);
        }

        .reward-icon {
            font-size: 80px;
            margin-bottom: 20px;
            animation: bounce 0.5s infinite alternate;
        }

        .reward-title {
            font-family: var(--font-pixel);
            font-size: 18px;
            margin-bottom: 10px;
            text-shadow: 2px 2px 0 rgba(255,255,255,0.5);
        }

        /* ===== GAME OVER ===== */
        .game-over-screen {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0,0,0,0.95);
            z-index: 9999;
            justify-content: center;
            align-items: center;
            flex-direction: column;
            backdrop-filter: blur(10px);
        }

        .game-over-screen.active {
            display: flex;
            animation: glitchIn 0.5s ease;
        }

        @keyframes glitchIn {
            0% { transform: scale(0.8); opacity: 0; filter: hue-rotate(90deg); }
            50% { transform: scale(1.05); filter: hue-rotate(180deg); }
            100% { transform: scale(1); opacity: 1; filter: hue-rotate(0deg); }
        }

        .game-over-content {
            text-align: center;
            padding: 40px;
            border: 4px solid var(--danger);
            border-radius: 8px;
            background: linear-gradient(135deg, #1a0000, #2d0000);
            box-shadow: 
                0 0 60px rgba(255,0,0,0.5),
                0 8px 0 #7b0000;
            max-width: 600px;
            width: 90%;
        }

        .game-over-title {
            font-family: var(--font-pixel);
            font-size: 36px;
            color: var(--danger);
            text-shadow: 4px 4px 0 #000;
            margin-bottom: 20px;
            animation: glitch 1s infinite;
        }

        .game-over-text {
            font-size: 18px;
            color: #fff;
            margin-bottom: 30px;
            line-height: 1.6;
        }

        .skull-icon {
            font-size: 100px;
            margin-bottom: 20px;
            animation: shake 2s infinite;
        }

        .hp-warning {
            animation: pulse-red 1s infinite;
        }

        @keyframes pulse-red {
            0%, 100% { box-shadow: 0 0 20px var(--danger); }
            50% { box-shadow: 0 0 40px var(--danger), 0 0 60px #ff0000; }
        }

        .confetti {
            position: fixed;
            width: 15px;
            height: 15px;
            top: -20px;
            z-index: 4000;
            animation: confetti-fall linear forwards;
        }

        @keyframes confetti-fall {
            to {
                transform: translateY(100vh) rotate(720deg);
                opacity: 0;
            }
        }

        /* ===== TUTORIAL ===== */
        #tutorial {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(135deg, #0f0c29, #302b63, #24243e);
            z-index: 2000;
            display: flex;
            justify-content: center;
            align-items: center;
            overflow-y: auto;
        }

        .tutorial-content {
            max-width: 1000px;
            padding: 40px;
            text-align: center;
        }

        .game-logo {
            font-family: var(--font-pixel);
            font-size: 48px;
            color: var(--accent);
            text-shadow: 
                4px 4px 0 #000,
                0 0 30px var(--accent),
                0 0 60px var(--cyber);
            margin-bottom: 30px;
            animation: neon-pulse 2s infinite;
        }

        .story-intro {
            background: rgba(0,0,0,0.6);
            border: 4px solid var(--magic);
            border-radius: 8px;
            padding: 30px;
            margin: 30px 0;
            font-size: 18px;
            line-height: 2;
            text-align: left;
            box-shadow: inset 0 0 30px rgba(255,0,255,0.1);
        }

        .feature-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 20px;
            margin: 30px 0;
        }

        .feature-card {
            background: linear-gradient(135deg, rgba(102,126,234,0.3), rgba(118,75,162,0.3));
            border: 3px solid var(--cyber);
            border-radius: 8px;
            padding: 25px;
            transition: all 0.3s;
            box-shadow: 0 4px 0 rgba(0,255,255,0.2);
        }

        .feature-card:hover {
            transform: translateY(-10px) scale(1.02);
            box-shadow: 
                0 14px 0 rgba(0,255,255,0.2),
                0 20px 40px rgba(0,255,255,0.2);
            border-color: var(--accent);
        }

        .feature-emoji {
            font-size: 50px;
            margin-bottom: 15px;
            display: block;
            animation: pixel-bounce 2s infinite;
        }

        /* ===== PHASE SELECT MAP ===== */
        .phase-map {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 20px;
            max-width: 700px;
            margin: 30px auto;
        }

        .phase-node {
            background: linear-gradient(180deg, #4a5568 0%, #2d3748 100%);
            border: 4px solid #718096;
            border-bottom: 8px solid #1a202c;
            border-radius: 8px;
            padding: 20px;
            cursor: pointer;
            transition: all 0.3s;
            position: relative;
            text-align: center;
        }

        .phase-node:hover {
            transform: translateY(-5px);
            border-color: var(--accent);
            box-shadow: 
                0 10px 0 #1a202c,
                0 0 30px rgba(0,255,136,0.4);
        }

        .phase-node.completed {
            background: linear-gradient(180deg, #48bb78 0%, #2f855a 100%);
            border-color: #68d391;
        }

        .phase-node.locked {
            opacity: 0.5;
            cursor: not-allowed;
            filter: grayscale(1);
        }

        .phase-number {
            font-family: var(--font-pixel);
            font-size: 24px;
            color: var(--warning);
            margin-bottom: 10px;
        }

        .phase-name {
            font-family: var(--font-pixel);
            font-size: 10px;
            color: white;
        }

        .phase-icon {
            font-size: 40px;
            margin-bottom: 10px;
        }

        /* ===== LOADING ===== */
        .loading-screen {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: #000;
            display: flex;
            justify-content: center;
            align-items: center;
            z-index: 9999;
            transition: opacity 0.5s;
        }

        .loading-text {
            font-family: var(--font-pixel);
            font-size: 20px;
            color: var(--accent);
            animation: neon-pulse 1s infinite;
        }

        .hidden {
            display: none !important;
        }

        .attempts-display {
            font-family: var(--font-pixel);
            font-size: 14px;
            color: var(--danger);
            text-align: center;
            margin: 10px 0;
            text-shadow: 2px 2px 0 #000;
        }

        .error-shake {
            animation: shake 0.5s;
        }

        .http-method-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 15px;
            margin: 20px 0;
        }

        .method-card {
            background: linear-gradient(180deg, #667eea 0%, #4a5568 100%);
            border: 4px solid white;
            border-bottom: 8px solid #2d3748;
            border-radius: 8px;
            padding: 20px;
            text-align: center;
            cursor: pointer;
            transition: all 0.3s;
            font-family: var(--font-pixel);
            font-size: 14px;
            box-shadow: 0 4px 0 #2d3748;
        }

        .method-card:hover {
            transform: scale(1.05) translateY(-4px);
            box-shadow: 
                0 8px 0 #2d3748,
                0 0 20px rgba(102,126,234,0.6);
        }

        .method-card.selected {
            background: var(--accent);
            color: black;
            border-color: var(--accent);
            box-shadow: 0 0 30px rgba(0,255,136,0.5);
        }

        .method-card.correct {
            background: #00ff88;
            color: black;
            animation: bounce 0.5s;
            border-color: #00ff88;
            box-shadow: 0 0 30px rgba(0,255,136,0.6);
        }

        .method-card.wrong {
            background: #ff4444;
            animation: shake 0.5s;
            border-color: #ff4444;
            box-shadow: 0 0 30px rgba(255,0,0,0.5);
        }

        .scenario-box {
            background: rgba(0,0,0,0.5);
            border: 3px solid var(--cyber);
            border-radius: 8px;
            padding: 20px;
            margin: 20px 0;
            font-size: 18px;
            text-align: center;
            box-shadow: inset 0 0 20px rgba(0,255,255,0.1);
        }

        .status-code-grid {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 15px;
            margin: 20px 0;
        }

        .status-card {
            background: linear-gradient(180deg, #2d3748 0%, #1a202c 100%);
            border: 4px solid #4a5568;
            border-bottom: 8px solid #1a202c;
            border-radius: 8px;
            padding: 15px;
            cursor: pointer;
            transition: all 0.3s;
            text-align: center;
            box-shadow: 0 4px 0 #1a202c;
        }

        .status-card:hover {
            transform: translateY(-5px);
            border-color: var(--cyber);
            box-shadow: 
                0 8px 0 #1a202c,
                0 10px 20px rgba(0,255,255,0.2);
        }

        .status-card.selected {
            border-color: var(--accent);
            background: rgba(0,255,136,0.2);
            box-shadow: 0 0 20px rgba(0,255,136,0.3);
        }

        .status-code {
            font-family: var(--font-pixel);
            font-size: 24px;
            color: var(--warning);
            margin-bottom: 5px;
        }

        .bug-display {
            background: #0a0a0a;
            border: 3px solid var(--danger);
            border-radius: 8px;
            padding: 20px;
            font-family: var(--font-code);
            font-size: 14px;
            color: #ff4444;
            margin: 15px 0;
            text-align: left;
            overflow-x: auto;
            box-shadow: inset 0 0 20px rgba(255,0,0,0.1);
        }

        .param-builder {
            background: rgba(0,0,0,0.5);
            border-radius: 8px;
            padding: 20px;
            margin: 15px 0;
            border: 2px solid rgba(255,255,255,0.1);
        }

        .param-input-group {
            display: flex;
            gap: 10px;
            margin: 10px 0;
            align-items: center;
            flex-wrap: wrap;
        }

        .param-key, .param-value {
            background: #0a0a0a;
            border: 3px solid var(--cyber);
            border-radius: 8px;
            padding: 12px;
            color: var(--accent);
            font-family: var(--font-code);
            font-size: 14px;
            flex: 1;
            min-width: 120px;
            box-shadow: inset 0 0 10px rgba(0,255,255,0.1);
        }

        .param-equals {
            font-size: 24px;
            color: var(--warning);
            font-weight: bold;
        }

        .url-preview {
            background: #0a0a0a;
            border: 3px solid var(--accent);
            border-radius: 8px;
            padding: 15px;
            font-family: var(--font-code);
            font-size: 16px;
            color: var(--accent);
            margin: 15px 0;
            word-break: break-all;
            min-height: 50px;
            box-shadow: inset 0 0 20px rgba(0,255,136,0.1);
        }

        .query-badge {
            background: var(--cyber);
            color: black;
            padding: 5px 10px;
            border-radius: 4px;
            font-size: 12px;
            font-weight: bold;
            margin: 0 5px;
            display: inline-block;
            font-family: var(--font-pixel);
            box-shadow: 0 2px 0 rgba(0,0,0,0.3);
        }

        @media (max-width: 768px) {
            .splash-logo {
                font-size: 20px;
            }

            .game-logo {
                font-size: 28px;
            }

            .modal-content {
                width: 95%;
                padding: 20px;
            }

            .character-avatar {
                width: 60px;
                height: 60px;
                font-size: 30px;
            }

            .memory-grid {
                grid-template-columns: repeat(3, 1fr);
            }

            .phase-map {
                grid-template-columns: repeat(2, 1fr);
            }

            .pixel-btn {
                font-size: 12px;
                padding: 15px 20px;
            }
        }
    </style>
<base target="_blank">
</head>
<body>
    <div class="loading-screen" id="loadingScreen">
        <div class="loading-text">CARREGANDO AVENTURA...</div>
    </div>

    <div class="game-over-screen" id="gameOverScreen">
        <div class="game-over-content">
            <div class="skull-icon">💀</div>
            <h1 class="game-over-title">GAME OVER</h1>
            <p class="game-over-text" id="gameOverText">
                O Sr. H4ck3r venceu desta vez!<br>
                Zé não conseguiu proteger seus dados...
            </p>
            <div style="margin-top: 20px;">
                <button class="btn btn-success" onclick="restartCurrentPhase()">
                    🔄 TENTAR NOVAMENTE
                </button>
                <button class="btn btn-warning" onclick="backToMenu()">
                    📋 MENU PRINCIPAL
                </button>
            </div>
        </div>
    </div>

    <!-- ===== SPLASH SCREEN / LOGIN ===== -->
    <div id="splashScreen">
        <div class="scanline-overlay"></div>

        <div class="pixel-lock-container">
            <div class="pixel-lock">
                <div class="pixel-lock-shackle"></div>
                <div class="pixel-lock-body">
                    <div class="pixel-lock-keyhole"></div>
                </div>
            </div>
        </div>

        <h1 class="splash-logo">🌐 HTTP QUEST</h1>
        <p class="splash-subtitle">A LENDA DO GUARDIÃO DA WEB</p>

        <div class="splash-buttons">
            <button class="pixel-btn pixel-btn-start" onclick="showTutorial()">
                <span class="pixel-decoration">🚀</span> INICIAR AVENTURA <span class="pixel-decoration">🚀</span>
            </button>
            <button class="pixel-btn" onclick="showPhaseSelect()">
                <span class="pixel-decoration">🗺️</span> MAPA DE FASES <span class="pixel-decoration">🗺️</span>
            </button>
            <button class="pixel-btn" onclick="showHowToPlay()">
                <span class="pixel-decoration">📖</span> COMO JOGAR <span class="pixel-decoration">📖</span>
            </button>
        </div>

        <p style="margin-top: 30px; color: #888; font-size: 12px; font-family: var(--font-pixel);">
            💡 CLIQUE NOS PERSONAGENS PARA CONVERSAR!
        </p>
    </div>

    <!-- ===== PHASE SELECT ===== -->
    <div id="phaseSelect" style="display:none; position:fixed; top:0; left:0; width:100%; height:100%; background:linear-gradient(135deg, #0f0c29, #302b63, #24243e); z-index:2500; overflow-y:auto; padding:40px;">
        <div style="text-align:center; max-width:800px; margin:0 auto;">
            <h1 style="font-family:var(--font-pixel); font-size:28px; color:var(--accent); text-shadow:4px 4px 0 #000; margin-bottom:30px;">
                🗺️ MAPA DAS FASES
            </h1>

            <div class="phase-map" id="phaseMap">
                <div class="phase-node" onclick="startPhaseFromMap(1)">
                    <div class="phase-icon">📡</div>
                    <div class="phase-number">01</div>
                    <div class="phase-name">O RESGATE DO HTTP</div>
                </div>
                <div class="phase-node locked" id="phaseNode2">
                    <div class="phase-icon">🛡️</div>
                    <div class="phase-number">02</div>
                    <div class="phase-name">A ARMADURA HTTPS</div>
                </div>
                <div class="phase-node locked" id="phaseNode3">
                    <div class="phase-icon">🌐</div>
                    <div class="phase-number">03</div>
                    <div class="phase-name">LABIRINTO DA URL</div>
                </div>
                <div class="phase-node locked" id="phaseNode4">
                    <div class="phase-icon">📡</div>
                    <div class="phase-number">04</div>
                    <div class="phase-name">CICLO REQUEST/RESPONSE</div>
                </div>
                <div class="phase-node locked" id="phaseNode5">
                    <div class="phase-icon">🐛</div>
                    <div class="phase-number">05</div>
                    <div class="phase-name">CAÇADOR DE BUGS</div>
                </div>
                <div class="phase-node locked" id="phaseNode6">
                    <div class="phase-icon">⚙️</div>
                    <div class="phase-number">06</div>
                    <div class="phase-name">MESTRE DOS PARÂMETROS</div>
                </div>
            </div>

            <button class="pixel-btn" onclick="backToSplash()" style="margin-top:30px;">
                🔙 VOLTAR
            </button>
        </div>
    </div>

    <!-- ===== HOW TO PLAY ===== -->
    <div id="howToPlay" style="display:none; position:fixed; top:0; left:0; width:100%; height:100%; background:linear-gradient(135deg, #0f0c29, #302b63, #24243e); z-index:2500; overflow-y:auto; padding:40px;">
        <div style="text-align:center; max-width:800px; margin:0 auto;">
            <h1 style="font-family:var(--font-pixel); font-size:28px; color:var(--accent); text-shadow:4px 4px 0 #000; margin-bottom:30px;">
                📖 COMO JOGAR
            </h1>

            <div class="feature-grid">
                <div class="feature-card">
                    <span class="feature-emoji">❤️</span>
                    <h3 style="font-family:var(--font-pixel); font-size:12px; color:var(--warning); margin-bottom:10px;">VIDA (HP)</h3>
                    <p>Você começa com 100 HP. Erros em desafios custam vida. Se chegar a 0, é GAME OVER!</p>
                </div>
                <div class="feature-card">
                    <span class="feature-emoji">⭐</span>
                    <h3 style="font-family:var(--font-pixel); font-size:12px; color:var(--warning); margin-bottom:10px;">XP E NÍVEIS</h3>
                    <p>Ganhe XP completando fases. A cada 300 XP, você sobe de nível!</p>
                </div>
                <div class="feature-card">
                    <span class="feature-emoji">🏆</span>
                    <h3 style="font-family:var(--font-pixel); font-size:12px; color:var(--warning); margin-bottom:10px;">CONQUISTAS</h3>
                    <p>Desbloqueie badges épicos ao completar desafios. Colete todos!</p>
                </div>
                <div class="feature-card">
                    <span class="feature-emoji">🎮</span>
                    <h3 style="font-family:var(--font-pixel); font-size:12px; color:var(--warning); margin-bottom:10px;">MINI-GAMES</h3>
                    <p>Cada fase tem um mini-game diferente: memória, digitação, quiz e mais!</p>
                </div>
            </div>

            <div class="story-intro" style="text-align:left; margin-top:30px;">
                <p style="margin-bottom:15px;"><strong>🎭 CONTROLES:</strong></p>
                <p>• Clique nos <strong>personagens</strong> para conversar com eles</p>
                <p>• Complete os <strong>mini-games</strong> para avançar de fase</p>
                <p>• Cuidado com o <strong>Sr. H4ck3r</strong>! Ele aparece nas fases mais difíceis!</p>
                <p>• Use o <strong>Mapa de Fases</strong> para navegar entre os níveis</p>
            </div>

            <button class="pixel-btn" onclick="backToSplash()" style="margin-top:30px;">
                🔙 VOLTAR
            </button>
        </div>
    </div>

    <div id="gameContainer">
        <div id="hud">
            <div class="hud-section">
                <div class="stat-box">
                    <span class="stat-icon">❤️</span>
                    <div class="hp-bar" id="hpBarContainer">
                        <div class="hp-fill" id="hpBar" style="width: 100%"></div>
                        <span class="bar-text" id="hpText">100/100</span>
                    </div>
                </div>
                <div class="stat-box">
                    <span class="stat-icon">⭐</span>
                    <div class="xp-bar">
                        <div class="xp-fill" id="xpBar" style="width: 0%"></div>
                        <span class="bar-text" id="xpText">Nível 1</span>
                    </div>
                </div>
            </div>

            <div class="level-badge" id="levelBadge">
                🎮 FASE 1
            </div>

            <div class="hud-section">
                <div class="stat-box">
                    <span class="stat-icon">🏆</span>
                    <span id="badgeCount">0</span>
                </div>
            </div>
        </div>

        <div id="gameWorld">
            <div class="cyber-grid"></div>

            <div class="character client-char" id="client" onclick="interactWith('client')">
                <div class="character-avatar">🖥️</div>
                <div class="character-name">Zé, o Navegador</div>
                <div class="dialogue-box" id="clientDialogue"></div>
            </div>

            <div class="character server-char" id="server" onclick="interactWith('server')">
                <div class="character-avatar">🖧</div>
                <div class="character-name">Severino, o Servidor</div>
                <div class="dialogue-box" id="serverDialogue"></div>
            </div>

            <div class="character hacker-char" id="hacker">
                <div class="character-avatar">👤</div>
                <div class="character-name">Sr. H4ck3r Maligno</div>
                <div class="dialogue-box" id="hackerDialogue"></div>
            </div>
        </div>
    </div>

    <div id="tutorial">
        <div class="tutorial-content">
            <div class="pixel-lock-container" style="margin-bottom:20px;">
                <div class="pixel-lock">
                    <div class="pixel-lock-shackle"></div>
                    <div class="pixel-lock-body">
                        <div class="pixel-lock-keyhole"></div>
                    </div>
                </div>
            </div>

            <h1 class="game-logo">🌐 HTTP QUEST</h1>
            <h2 style="font-family: var(--font-pixel); color: var(--cyber); margin-bottom: 20px;">A LENDA DO GUARDIÃO DA WEB</h2>

            <div class="story-intro">
                <p style="margin-bottom: 15px;">🎭 <strong>Bem-vindo à Cidade Digital!</strong></p>
                <p>Zé, o Navegador, precisa enviar mensagens importantes para Severino, o Servidor. Mas há um problema: <span style="color: var(--danger);">o malvado Sr. H4ck3r está interceptando tudo!</span></p>
                <p style="margin-top: 15px;">Sua missão: <span style="color: var(--accent);">Proteger a comunicação usando os poderes do HTTP e HTTPS!</span> Cuidado: <span style="color: var(--danger);">erros custam HP!</span> Se chegar a 0, é GAME OVER! 🛡️</p>
            </div>

            <div class="feature-grid">
                <div class="feature-card">
                    <span class="feature-emoji">🎮</span>
                    <h3 style="font-family: var(--font-pixel); font-size: 12px; color: var(--warning); margin-bottom: 10px;">6 Fases Épicas</h3>
                    <p>Do zero ao herói! Aprenda HTTP, HTTPS, URLs, Request/Response, Debugging e Parâmetros!</p>
                </div>
                <div class="feature-card">
                    <span class="feature-emoji">🧩</span>
                    <h3 style="font-family: var(--font-pixel); font-size: 12px; color: var(--warning); margin-bottom: 10px;">Mini-Games</h3>
                    <p>Memória, digitação rápida, quebra-cabeças, quiz e desafios práticos!</p>
                </div>
                <div class="feature-card">
                    <span class="feature-emoji">❤️</span>
                    <h3 style="font-family: var(--font-pixel); font-size: 12px; color: var(--warning); margin-bottom: 10px;">Cuidado!</h3>
                    <p>Erros custam HP! Proteja Zé a todo custo!</p>
                </div>
                <div class="feature-card">
                    <span class="feature-emoji">🏆</span>
                    <h3 style="font-family: var(--font-pixel); font-size: 12px; color: var(--warning); margin-bottom: 10px;">Conquistas</h3>
                    <p>Desbloqueie badges épicos e prove que você é o mestre!</p>
                </div>
            </div>

            <button class="pixel-btn pixel-btn-start" onclick="startAdventure()" style="font-size: 16px; padding: 25px 50px;">
                🚀 INICIAR AVENTURA!
            </button>

            <button class="pixel-btn" onclick="backToSplashFromTutorial()" style="margin-top:15px;">
                🔙 VOLTAR AO MENU
            </button>
        </div>
    </div>

    <div class="modal" id="phase1Modal">
        <div class="modal-content">
            <div class="modal-header">
                <h2 class="modal-title">🚨 FASE 1: O RESGATE DO HTTP</h2>
                <p class="modal-subtitle">Zé precisa entender como funciona a web! (Erros custam 20 HP)</p>
            </div>

            <div class="story-container">
                <p class="story-text">
                    <strong>Zé (o Navegador):</strong> "Ei, Guardião! Preciso enviar uma mensagem para o Severino, mas não faço ideia de como! Me ajuda?!"
                </p>
            </div>

            <div class="attempts-display" id="phase1Attempts">
                ❤️ Tentativas restantes: <span id="attemptsCount">3</span>
            </div>

            <div class="challenge-container">
                <div class="challenge-title">🎮 DESAFIO: Jogo da Memória HTTP</div>
                <p style="margin-bottom: 15px;">Encontre os pares! Cada erro custa <span style="color: var(--danger);">20 HP</span>:</p>

                <div class="memory-grid" id="memoryGrid"></div>

                <div id="memoryFeedback" style="margin-top: 20px; text-align: center; font-size: 18px; min-height: 30px;"></div>
            </div>

            <div style="text-align: center; margin-top: 20px;">
                <button class="btn" onclick="skipPhase(1)" style="font-size: 10px;">Pular (perde XP)</button>
            </div>
        </div>
    </div>

    <div class="modal" id="phase2Modal">
        <div class="modal-content">
            <div class="modal-header">
                <h2 class="modal-title">🛡️ FASE 2: A ARMADURA HTTPS</h2>
                <p class="modal-subtitle">O Sr. H4ck3r apareceu! Erros ou tempo esgotado = -30 HP!</p>
            </div>

            <div class="story-container">
                <p class="story-text">
                    <strong>Sr. H4ck3r:</strong> "Mwahaha! Vou interceptar todos os dados! Seus pacotes serão MEUS! 💀"
                </p>
                <p class="story-text" style="margin-top: 10px; color: var(--accent);">
                    <strong>Severino:</strong> "Guardião! Ative o HTTPS! É nossa única chance!"
                </p>
            </div>

            <div class="attempts-display" id="phase2Attempts">
                ⏱️ Tempo: <span style="color: var(--cyber);">10s</span> | ❤️ Erro: <span style="color: var(--danger);">-30 HP</span>
            </div>

            <div class="challenge-container">
                <div class="challenge-title">⚡ DESAFIO: Digitação Defensiva</div>
                <p>Digite o código de ativação do HTTPS antes que o hacker intercepte!</p>

                <div class="typing-challenge">
                    <div class="typing-target" id="typingTarget">https://modo-seguro-ativado</div>
                    <input type="text" class="typing-input" id="typingInput" placeholder="Digite aqui..." autocomplete="off">
                </div>

                <div style="display: flex; justify-content: space-between; margin-top: 15px; font-family: var(--font-pixel); font-size: 12px;">
                    <span style="color: var(--danger);">⏱️ Tempo: <span id="typingTimer">10</span>s</span>
                    <span style="color: var(--accent);">🔒 Segurança: <span id="securityLevel">0</span>%</span>
                </div>
            </div>

            <div id="phase2Result" style="text-align: center; margin-top: 20px;"></div>
        </div>
    </div>

    <div class="modal" id="phase3Modal">
        <div class="modal-content">
            <div class="modal-header">
                <h2 class="modal-title">🌐 FASE 3: O LABIRINTO DA URL</h2>
                <p class="modal-subtitle">Monte o caminho correto! Erros custam 25 HP!</p>
            </div>

            <div class="story-container">
                <p class="story-text">
                    <strong>Zé:</strong> "Estou perdido no labirinto da internet! Como chego ao servidor?"
                </p>
            </div>

            <div class="attempts-display" id="phase3Attempts">
                ❤️ Erros custam: <span style="color: var(--danger);">-25 HP</span> | Tentativas: <span id="urlAttempts">0</span>/3
            </div>

            <div class="challenge-container">
                <div class="challenge-title">🧩 DESAFIO: Quebra-Cabeça da URL</div>
                <p>Clique nas peças na ordem correta para formar uma URL completa:</p>

                <div class="drop-zone" id="urlDropZone" style="min-height: 80px; font-family: var(--font-pixel); font-size: 16px; color: var(--accent);">
                    <span style="color: #666; font-size: 12px;">A URL aparecerá aqui...</span>
                </div>

                <div class="drag-container" id="urlPieces">
                    <div class="drag-item" onclick="addUrlPiece(this, 'https://')" data-piece="https://">🔒 https://</div>
                    <div class="drag-item" onclick="addUrlPiece(this, 'www.')" data-piece="www.">🌐 www.</div>
                    <div class="drag-item" onclick="addUrlPiece(this, 'google')" data-piece="google">🏢 google</div>
                    <div class="drag-item" onclick="addUrlPiece(this, '.com')" data-piece=".com">📍 .com</div>
                    <div class="drag-item" onclick="addUrlPiece(this, '/search')" data-piece="/search">🔍 /search</div>
                    <div class="drag-item" onclick="addUrlPiece(this, '?q=gatos')" data-piece="?q=gatos">❓ ?q=gatos</div>
                </div>

                <div style="text-align: center; margin-top: 20px;">
                    <button class="btn" onclick="checkUrlPuzzle()">Verificar URL</button>
                    <button class="btn btn-danger" onclick="resetUrlPuzzle()">🔄 Reiniciar</button>
                </div>
            </div>

            <div id="urlResult" style="text-align: center; margin-top: 15px; font-size: 18px;"></div>
        </div>
    </div>

    <div class="modal" id="phase4Modal">
        <div class="modal-content">
            <div class="modal-header">
                <h2 class="modal-title">📡 FASE 4: O CICLO DO PEDIDO E RESPOSTA</h2>
                <p class="modal-subtitle">Zé aprende a conversar com o servidor! Erros = -20 HP!</p>
            </div>

            <div class="story-container">
                <p class="story-text">
                    <strong>Zé:</strong> "Guardião, eu envio a mensagem, mas o Severino responde coisas diferentes... como sei o que ele está dizendo?!"
                </p>
                <p class="story-text" style="margin-top: 10px;">
                    <strong>Severino:</strong> "Cada método HTTP tem uma resposta específica! Aprenda o ciclo completo!"
                </p>
            </div>

            <div class="attempts-display" id="phase4Attempts">
                ❤️ Vida por erro: <span style="color: var(--danger);">-20 HP</span> | Desafios: <span id="reqResProgress">0</span>/3
            </div>

            <div class="challenge-container">
                <div class="challenge-title">🔄 DESAFIO: Métodos HTTP & Respostas</div>
                <p>Leia o cenário e escolha o <strong>MÉTODO HTTP</strong> correto:</p>

                <div class="scenario-box" id="scenarioBox">
                    <strong>Cenário 1/3:</strong><br>
                    Zé quer <span style="color: var(--cyber);">VER</span> a página inicial do site. Qual método usar?
                </div>

                <div class="http-method-grid" id="methodGrid">
                    <div class="method-card" onclick="selectMethod('GET')">
                        <div style="font-size: 30px;">👁️</div>
                        <div>GET</div>
                        <small style="font-size: 8px;">Buscar dados</small>
                    </div>
                    <div class="method-card" onclick="selectMethod('POST')">
                        <div style="font-size: 30px;">✉️</div>
                        <div>POST</div>
                        <small style="font-size: 8px;">Enviar dados</small>
                    </div>
                    <div class="method-card" onclick="selectMethod('DELETE')">
                        <div style="font-size: 30px;">🗑️</div>
                        <div>DELETE</div>
                        <small style="font-size: 8px;">Apagar dados</small>
                    </div>
                </div>

                <div id="methodFeedback" style="text-align: center; margin-top: 20px; font-size: 18px; min-height: 30px;"></div>
            </div>
        </div>
    </div>

    <div class="modal" id="phase5Modal">
        <div class="modal-content">
            <div class="modal-header">
                <h2 class="modal-title">🐛 FASE 5: O CAÇADOR DE BUGS</h2>
                <p class="modal-subtitle">Depurando Requisições HTTP! Erros = -25 HP!</p>
            </div>

            <div class="story-container">
                <p class="story-text">
                    <strong>Sr. H4ck3r:</strong> "Hehehe... deixei bugs espalhados pelo seu código! Mwahaha! 💀"
                </p>
                <p class="story-text" style="margin-top: 10px; color: var(--accent);">
                    <strong>Severino:</strong> "Guardião! Precisamos identificar os códigos de status HTTP para consertar os bugs!"
                </p>
            </div>

            <div class="attempts-display" id="phase5Attempts">
                ❤️ Erro: <span style="color: var(--danger);">-25 HP</span> | Bugs restantes: <span id="bugsRemaining">3</span>
            </div>

            <div class="challenge-container">
                <div class="challenge-title">🔍 DESAFIO: Identifique o Status Code</div>
                <p>Leia o erro abaixo e selecione o código de status HTTP correto:</p>

                <div class="bug-display" id="bugDisplay">
                    <div style="color: #888;">// Erro #1:</div>
                    <div>A página que Zé tentou acessar não existe no servidor!</div>
                    <div style="color: #666; margin-top: 10px;">Servidor respondeu: ???</div>
                </div>

                <div class="status-code-grid" id="statusGrid">
                    <div class="status-card" onclick="selectStatus(200, 'OK')">
                        <div class="status-code">200</div>
                        <div style="font-size: 14px;">OK - Sucesso</div>
                    </div>
                    <div class="status-card" onclick="selectStatus(404, 'Not Found')">
                        <div class="status-code">404</div>
                        <div style="font-size: 14px;">Not Found</div>
                    </div>
                    <div class="status-card" onclick="selectStatus(500, 'Server Error')">
                        <div class="status-code">500</div>
                        <div style="font-size: 14px;">Erro do Servidor</div>
                    </div>
                    <div class="status-card" onclick="selectStatus(403, 'Forbidden')">
                        <div class="status-code">403</div>
                        <div style="font-size: 14px;">Proibido/Forbidden</div>
                    </div>
                </div>

                <div id="statusFeedback" style="text-align: center; margin-top: 20px; font-size: 18px;"></div>
            </div>
        </div>
    </div>

    <div class="modal" id="phase6Modal">
        <div class="modal-content">
            <div class="modal-header">
                <h2 class="modal-title">⚙️ FASE 6: MESTRE DOS PARÂMETROS</h2>
                <p class="modal-subtitle">Parâmetros de Requisição e Resposta! Erros = -20 HP!</p>
            </div>

            <div class="story-container">
                <p class="story-text">
                    <strong>Zé:</strong> "Preciso enviar dados específicos para o servidor: meu nome e minha idade! Como faço?"
                </p>
                <p class="story-text" style="margin-top: 10px;">
                    <strong>Severino:</strong> "Use parâmetros de query na URL! Formato: <code>?chave=valor&chave2=valor2</code>"
                </p>
            </div>

            <div class="attempts-display" id="phase6Attempts">
                ❤️ Erro: <span style="color: var(--danger);">-20 HP</span> | Tentativas: <span id="paramAttempts">0</span>/3
            </div>

            <div class="challenge-container">
                <div class="challenge-title">🧬 DESAFIO: Construtor de Parâmetros</div>
                <p>Monte a URL com os parâmetros corretos:</p>

                <div style="background: rgba(0,0,0,0.3); padding: 15px; border-radius: 8px; margin: 15px 0;">
                    <strong style="color: var(--cyber);">Missão:</strong> Crie uma URL que envie:<br>
                    • nome = <strong>Zé</strong><br>
                    • idade = <strong>25</strong>
                </div>

                <div class="url-preview" id="urlPreview">
                    https://api.exemplo.com/cadastro<span style="color: var(--warning);"></span>
                </div>

                <div class="param-builder">
                    <div style="margin-bottom: 10px; color: var(--cyber);">Adicione os parâmetros:</div>

                    <div class="param-input-group">
                        <input type="text" class="param-key" id="paramKey1" placeholder="chave (ex: nome)" value="">
                        <span class="param-equals">=</span>
                        <input type="text" class="param-value" id="paramValue1" placeholder="valor (ex: Zé)" value="">
                    </div>

                    <div style="text-align: center; margin: 10px 0; color: var(--warning);">&</div>

                    <div class="param-input-group">
                        <input type="text" class="param-key" id="paramKey2" placeholder="chave 2" value="">
                        <span class="param-equals">=</span>
                        <input type="text" class="param-value" id="paramValue2" placeholder="valor 2" value="">
                    </div>

                    <div style="text-align: center; margin-top: 20px;">
                        <button class="btn" onclick="buildParams()">🔨 Construir URL</button>
                        <button class="btn btn-danger" onclick="resetParams()">🔄 Limpar</button>
                    </div>
                </div>

                <div id="paramFeedback" style="text-align: center; margin-top: 20px; font-size: 18px;"></div>
            </div>
        </div>
    </div>

    <div class="reward-popup" id="rewardPopup">
        <div class="reward-icon" id="rewardIcon">🏆</div>
        <div class="reward-title" id="rewardTitle">CONQUISTA!</div>
        <div id="rewardText">Você desbloqueou algo épico!</div>
        <button class="btn btn-success" onclick="closeReward()" style="margin-top: 20px;">COLETAR!</button>
    </div>

    <script>
        const gameState = {
            currentPhase: 1,
            hp: 100,
            maxHp: 100,
            xp: 0,
            level: 1,
            badges: [],
            completedPhases: [],
            memoryMatches: 0,
            urlPiecesOrder: [],
            urlAttempts: 0,
            phaseAttempts: {
                1: 3,
                2: 1,
                3: 3,
                4: 3,
                5: 3,
                6: 3
            },
            typingTimer: null,
            gameActive: true,
            currentScenario: 0,
            methodScenarios: [
                { text: "Zé quer <span style='color: var(--cyber);'>VER</span> a página inicial do site. Qual método usar?", answer: 'GET', icon: '👁️' },
                { text: "Zé quer <span style='color: var(--cyber);'>ENVIAR</span> um formulário de cadastro. Qual método usar?", answer: 'POST', icon: '✉️' },
                { text: "Zé quer <span style='color: var(--cyber);'>APAGAR</span> uma foto do servidor. Qual método usar?", answer: 'DELETE', icon: '🗑️' }
            ],
            currentBug: 0,
            bugs: [
                { text: "A página que Zé tentou acessar não existe no servidor!", code: 404, name: "Not Found" },
                { text: "O servidor está funcionando perfeitamente e enviou a página!", code: 200, name: "OK" },
                { text: "O servidor queimou! Erro interno grave!", code: 500, name: "Server Error" }
            ]
        };

        const characters = {
            client: {
                name: 'Zé, o Navegador',
                dialogues: [
                    "Ei! Me ajuda a mandar essa mensagem? 📨",
                    "O que é HTTP mesmo? 🤔",
                    "Tô com medo desse hacker! 😰",
                    "Você é um ótimo Guardião! 🌟",
                    "GET ou POST? Sempre esqueço! 🤯",
                    "Status 404... página não encontrada? 😢"
                ]
            },
            server: {
                name: 'Severino, o Servidor',
                dialogues: [
                    "Aguardando conexão segura... 🖧",
                    "HTTPS ativado! Estamos protegidos! 🛡️",
                    "Status 200: Tudo OK! ✅",
                    "Você aprende rápido, jovem! 🎓",
                    "POST recebido! Processando dados... 💾",
                    "Query parameters recebidos com sucesso! 📋"
                ]
            },
            hacker: {
                name: 'Sr. H4ck3r',
                dialogues: [
                    "Mwahaha! Vou roubar seus dados! 💀",
                    "HTTP sem proteção é meu! 🎣",
                    "Não escapa do Sr. H4ck3r! 👤",
                    "Maldito HTTPS! Me estragou os planos! 😤",
                    "Erros 500 são meus favoritos! 🔥",
                    "Vou interceptar seus parâmetros! 👀"
                ]
            }
        };

        window.onload = function() {
            setTimeout(() => {
                document.getElementById('loadingScreen').style.opacity = '0';
                setTimeout(() => {
                    document.getElementById('loadingScreen').classList.add('hidden');
                }, 500);
            }, 1500);
        };

        // ===== SPLASH SCREEN FUNCTIONS =====
        function showTutorial() {
            document.getElementById('splashScreen').style.display = 'none';
            document.getElementById('tutorial').classList.remove('hidden');
        }

        function backToSplashFromTutorial() {
            document.getElementById('tutorial').classList.add('hidden');
            document.getElementById('splashScreen').style.display = 'flex';
        }

        function showPhaseSelect() {
            document.getElementById('splashScreen').style.display = 'none';
            document.getElementById('phaseSelect').style.display = 'block';
            updatePhaseMap();
        }

        function backToSplash() {
            document.getElementById('phaseSelect').style.display = 'none';
            document.getElementById('howToPlay').style.display = 'none';
            document.getElementById('splashScreen').style.display = 'flex';
        }

        function showHowToPlay() {
            document.getElementById('splashScreen').style.display = 'none';
            document.getElementById('howToPlay').style.display = 'block';
        }

        function updatePhaseMap() {
            for (let i = 1; i <= 6; i++) {
                const node = document.getElementById('phaseNode' + i) || document.querySelector('.phase-node:nth-child(' + i + ')');
                if (node) {
                    if (gameState.completedPhases.includes(i)) {
                        node.classList.add('completed');
                        node.classList.remove('locked');
                    } else if (i === 1 || gameState.completedPhases.includes(i - 1)) {
                        node.classList.remove('locked');
                    }
                }
            }
        }

        function startPhaseFromMap(phase) {
            const node = document.getElementById('phaseNode' + phase);
            if (node && node.classList.contains('locked')) {
                return;
            }
            document.getElementById('phaseSelect').style.display = 'none';
            startPhase(phase);
        }

        function takeDamage(amount) {
            if (!gameState.gameActive) return;

            gameState.hp -= amount;
            if (gameState.hp < 0) gameState.hp = 0;

            updateHUD();

            document.body.style.animation = 'shake 0.5s';
            document.getElementById('hpBarContainer').classList.add('hp-warning');

            setTimeout(() => {
                document.body.style.animation = '';
                document.getElementById('hpBarContainer').classList.remove('hp-warning');
            }, 500);

            if (gameState.hp <= 0) {
                triggerGameOver();
            }
        }

        function heal(amount) {
            gameState.hp += amount;
            if (gameState.hp > gameState.maxHp) gameState.hp = gameState.maxHp;
            updateHUD();
        }

        function triggerGameOver() {
            gameState.gameActive = false;

            if (gameState.typingTimer) {
                clearInterval(gameState.typingTimer);
                gameState.typingTimer = null;
            }

            document.querySelectorAll('.modal.active').forEach(modal => {
                modal.classList.remove('active');
            });

            setTimeout(() => {
                document.getElementById('gameOverScreen').classList.add('active');

                const texts = [
                    "O Sr. H4ck3r interceptou todos os pacotes! Zé está vulnerável...",
                    "O tempo acabou! O HTTPS não foi ativado a tempo...",
                    "Zé se perdeu no labirinto da URL e caiu na armadilha do hacker!",
                    "Zé não entendeu o protocolo HTTP! O servidor rejeitou a conexão...",
                    "Muitos bugs não corrigidos! O sistema travou!",
                    "Parâmetros incorretos! O servidor retornou erro 400!"
                ];
                document.getElementById('gameOverText').innerHTML = 
                    texts[gameState.currentPhase - 1] || "O Sr. H4ck3r venceu desta vez!";
            }, 500);
        }

        function restartCurrentPhase() {
            gameState.gameActive = true;
            gameState.hp = gameState.maxHp;
            gameState.urlPiecesOrder = [];
            gameState.urlAttempts = 0;
            gameState.currentScenario = 0;
            gameState.currentBug = 0;
            gameState.phaseAttempts = { 1: 3, 2: 1, 3: 3, 4: 3, 5: 3, 6: 3 };

            updateHUD();

            document.getElementById('gameOverScreen').classList.remove('active');

            setTimeout(() => {
                startPhase(gameState.currentPhase);
            }, 300);
        }

        function backToMenu() {
            location.reload();
        }

        function interactWith(char) {
            if (!gameState.gameActive) return;

            const charData = characters[char];
            const dialogueBox = document.getElementById(char + 'Dialogue');
            const randomDialogue = charData.dialogues[Math.floor(Math.random() * charData.dialogues.length)];

            dialogueBox.textContent = randomDialogue;
            dialogueBox.classList.add('active');

            createParticles(event.clientX, event.clientY, '✨');

            setTimeout(() => {
                dialogueBox.classList.remove('active');
            }, 3000);
        }

        function createParticles(x, y, emoji) {
            for (let i = 0; i < 5; i++) {
                setTimeout(() => {
                    const particle = document.createElement('div');
                    particle.textContent = emoji;
                    particle.style.position = 'fixed';
                    particle.style.left = x + 'px';
                    particle.style.top = y + 'px';
                    particle.style.fontSize = '24px';
                    particle.style.pointerEvents = 'none';
                    particle.style.zIndex = '9999';
                    particle.style.animation = `particleFloat 1s ease-out forwards`;
                    document.body.appendChild(particle);

                    setTimeout(() => particle.remove(), 1000);
                }, i * 100);
            }
        }

        function startAdventure() {
            document.getElementById('tutorial').style.animation = 'fadeOut 0.5s forwards';
            setTimeout(() => {
                document.getElementById('tutorial').classList.add('hidden');
                document.getElementById('splashScreen').style.display = 'none';
                startPhase(1);
            }, 500);
        }

        function startPhase(phase) {
            if (!gameState.gameActive) return;

            gameState.currentPhase = phase;
            document.getElementById('levelBadge').textContent = `🎮 FASE ${phase}`;

            if (phase === 2 || phase === 5) {
                document.getElementById('hacker').classList.add('visible');
            } else {
                document.getElementById('hacker').classList.remove('visible');
            }

            if (phase === 1) {
                gameState.phaseAttempts[1] = 3;
                updateAttemptsDisplay(1);
                initMemoryGame();
                document.getElementById('phase1Modal').classList.add('active');
            } else if (phase === 2) {
                document.getElementById('phase2Modal').classList.add('active');
                initTypingChallenge();
            } else if (phase === 3) {
                gameState.urlPiecesOrder = [];
                gameState.urlAttempts = 0;
                updateAttemptsDisplay(3);
                resetUrlPuzzle();
                document.getElementById('phase3Modal').classList.add('active');
            } else if (phase === 4) {
                gameState.currentScenario = 0;
                gameState.phaseAttempts[4] = 3;
                updateAttemptsDisplay(4);
                initRequestResponseGame();
                document.getElementById('phase4Modal').classList.add('active');
            } else if (phase === 5) {
                gameState.currentBug = 0;
                gameState.phaseAttempts[5] = 3;
                updateAttemptsDisplay(5);
                initDebuggingGame();
                document.getElementById('phase5Modal').classList.add('active');
            } else if (phase === 6) {
                gameState.phaseAttempts[6] = 3;
                updateAttemptsDisplay(6);
                resetParams();
                document.getElementById('phase6Modal').classList.add('active');
            }
        }

        function updateAttemptsDisplay(phase) {
            if (phase === 1) document.getElementById('attemptsCount').textContent = gameState.phaseAttempts[1];
            if (phase === 3) document.getElementById('urlAttempts').textContent = gameState.urlAttempts;
            if (phase === 4) document.getElementById('reqResProgress').textContent = gameState.currentScenario;
            if (phase === 5) document.getElementById('bugsRemaining').textContent = 3 - gameState.currentBug;
            if (phase === 6) document.getElementById('paramAttempts').textContent = 3 - gameState.phaseAttempts[6];
        }

        function initMemoryGame() {
            const grid = document.getElementById('memoryGrid');
            grid.innerHTML = '';
            gameState.memoryMatches = 0;

            const cards = [
                { icon: '📡', text: 'HTTP', pair: 1 },
                { icon: '🌐', text: 'Web', pair: 1 },
                { icon: '📨', text: 'Request', pair: 2 },
                { icon: '📬', text: 'Response', pair: 2 },
                { icon: '🔌', text: 'Port 80', pair: 3 },
                { icon: '🎯', text: 'Default', pair: 3 },
                { icon: '📝', text: 'Text', pair: 4 },
                { icon: '📄', text: 'Plain', pair: 4 }
            ];

            cards.sort(() => Math.random() - 0.5);

            let flippedCards = [];
            let matchedPairs = 0;

            cards.forEach((card, index) => {
                const cardEl = document.createElement('div');
                cardEl.className = 'memory-card';
                cardEl.dataset.index = index;
                cardEl.dataset.pair = card.pair;
                cardEl.textContent = '❓';

                cardEl.onclick = function() {
                    if (!gameState.gameActive) return;
                    if (flippedCards.length < 2 && !this.classList.contains('flipped') && !this.classList.contains('matched')) {
                        this.classList.add('flipped');
                        this.textContent = card.icon;
                        flippedCards.push({ el: this, pair: card.pair, text: card.text });

                        if (flippedCards.length === 2) {
                            setTimeout(() => checkMatch(), 500);
                        }
                    }
                };

                grid.appendChild(cardEl);
            });

            function checkMatch() {
                const [card1, card2] = flippedCards;

                if (card1.pair === card2.pair) {
                    card1.el.classList.add('matched');
                    card2.el.classList.add('matched');
                    matchedPairs++;

                    document.getElementById('memoryFeedback').innerHTML = 
                        `<span style="color: #00ff88;">✅ Par encontrado! ${card1.text} = ${card2.text}</span>`;

                    createParticles(window.innerWidth/2, window.innerHeight/2, '🎉');

                    if (matchedPairs === 4) {
                        setTimeout(() => {
                            showReward('🧠', 'Mestre da Memória!', 'Você completou o desafio da memória HTTP!');
                            addXP(100);
                            setTimeout(() => completePhase(1), 2000);
                        }, 1000);
                    }
                } else {
                    card1.el.classList.add('error');
                    card2.el.classList.add('error');

                    setTimeout(() => {
                        card1.el.classList.remove('flipped', 'error');
                        card2.el.classList.remove('flipped', 'error');
                        card1.el.textContent = '❓';
                        card2.el.textContent = '❓';
                    }, 800);

                    document.getElementById('memoryFeedback').innerHTML = 
                        `<span style="color: #ff4444;">❌ Erro! -20 HP</span>`;

                    takeDamage(20);
                    gameState.phaseAttempts[1]--;
                    updateAttemptsDisplay(1);
                }

                flippedCards = [];
            }
        }

        function initTypingChallenge() {
            const target = document.getElementById('typingTarget').textContent;
            const input = document.getElementById('typingInput');
            let timeLeft = 10;

            if (gameState.typingTimer) {
                clearInterval(gameState.typingTimer);
            }

            input.value = '';
            input.disabled = false;
            setTimeout(() => input.focus(), 100);

            document.getElementById('phase2Result').innerHTML = '';
            document.getElementById('typingTimer').textContent = timeLeft;
            document.getElementById('securityLevel').textContent = '0';

            gameState.typingTimer = setInterval(() => {
                if (!gameState.gameActive) {
                    clearInterval(gameState.typingTimer);
                    return;
                }

                timeLeft--;
                document.getElementById('typingTimer').textContent = timeLeft;

                if (timeLeft <= 0) {
                    clearInterval(gameState.typingTimer);
                    input.disabled = true;
                    document.getElementById('phase2Result').innerHTML = 
                        `<span style="color: #ff4444; font-size: 20px;">💀 TEMPO ESGOTADO!</span>`;

                    setTimeout(() => {
                        takeDamage(30);
                        if (gameState.gameActive) {
                            setTimeout(() => {
                                if (gameState.gameActive) initTypingChallenge();
                            }, 1500);
                        }
                    }, 500);
                }
            }, 1000);

            input.oninput = function() {
                if (!gameState.gameActive) return;

                const typed = this.value;
                let correct = 0;

                for (let i = 0; i < typed.length; i++) {
                    if (typed[i] === target[i]) correct++;
                }

                const percent = Math.floor((correct / target.length) * 100);
                document.getElementById('securityLevel').textContent = percent;

                if (typed === target) {
                    clearInterval(gameState.typingTimer);
                    input.disabled = true;
                    document.getElementById('phase2Result').innerHTML = 
                        `<span style="color: #00ff88; font-size: 20px;">🔒 HTTPS ATIVADO! Você protegeu a conexão!</span>`;
                    showReward('🛡️', 'Guardião Defensor!', 'Você ativou o modo seguro!');
                    addXP(150);
                    setTimeout(() => completePhase(2), 2000);
                }
            };
        }

        function addUrlPiece(element, piece) {
            if (!gameState.gameActive) return;
            if (element.classList.contains('used')) return;

            gameState.urlPiecesOrder.push(piece);
            element.classList.add('used');

            const dropZone = document.getElementById('urlDropZone');
            if (dropZone.querySelector('span')) {
                dropZone.innerHTML = '';
            }

            const pieceSpan = document.createElement('span');
            pieceSpan.textContent = piece;
            pieceSpan.style.color = 'var(--accent)';
            pieceSpan.style.margin = '0 2px';
            dropZone.appendChild(pieceSpan);
        }

        function checkUrlPuzzle() {
            if (!gameState.gameActive) return;

            const correct = ['https://', 'www.', 'google', '.com', '/search', '?q=gatos'];
            const isCorrect = JSON.stringify(gameState.urlPiecesOrder) === JSON.stringify(correct);

            if (isCorrect) {
                document.getElementById('urlResult').innerHTML = 
                    `<span style="color: #00ff88;">🎉 URL Perfeita! Zé chegou ao destino!</span>`;
                showReward('🌐', 'Arquiteto Web!', 'Você domina as URLs!');
                addXP(200);
                setTimeout(() => completePhase(3), 2000);
            } else {
                gameState.urlAttempts++;
                updateAttemptsDisplay(3);

                document.getElementById('urlResult').innerHTML = 
                    `<span style="color: #ff4444;">❌ URL incorreta! -25 HP<br>A ordem correta é: Protocolo → Subdomínio → Domínio → TLD → Caminho → Query</span>`;

                takeDamage(25);

                setTimeout(() => {
                    if (gameState.gameActive) {
                        resetUrlPuzzle();
                        document.getElementById('urlResult').innerHTML = '';
                    }
                }, 2000);
            }
        }

        function resetUrlPuzzle() {
            gameState.urlPiecesOrder = [];
            document.getElementById('urlDropZone').innerHTML = '<span style="color: #666; font-size: 12px;">A URL aparecerá aqui...</span>';
            document.getElementById('urlResult').innerHTML = '';

            document.querySelectorAll('#urlPieces .drag-item').forEach(item => {
                item.classList.remove('used');
            });
        }

        function initRequestResponseGame() {
            gameState.currentScenario = 0;
            loadScenario();
        }

        function loadScenario() {
            if (gameState.currentScenario >= gameState.methodScenarios.length) {
                showReward('📡', 'Comunicador HTTP!', 'Você domina o ciclo Request/Response!');
                addXP(250);
                setTimeout(() => completePhase(4), 2000);
                return;
            }

            const scenario = gameState.methodScenarios[gameState.currentScenario];
            document.getElementById('scenarioBox').innerHTML = 
                `<strong>Cenário ${gameState.currentScenario + 1}/3:</strong><br>${scenario.text}`;

            document.getElementById('methodFeedback').innerHTML = '';
            updateAttemptsDisplay(4);

            document.querySelectorAll('.method-card').forEach(card => {
                card.classList.remove('selected', 'correct', 'wrong');
            });
        }

        function selectMethod(method) {
            if (!gameState.gameActive) return;

            const scenario = gameState.methodScenarios[gameState.currentScenario];
            const cards = document.querySelectorAll('.method-card');

            cards.forEach(card => card.classList.remove('selected'));
            event.currentTarget.classList.add('selected');

            if (method === scenario.answer) {
                event.currentTarget.classList.add('correct');
                document.getElementById('methodFeedback').innerHTML = 
                    `<span style="color: #00ff88;">✅ Correto! ${method} é o método certo para ${scenario.icon}</span>`;

                createParticles(window.innerWidth/2, window.innerHeight/2, scenario.icon);

                setTimeout(() => {
                    gameState.currentScenario++;
                    loadScenario();
                }, 1500);
            } else {
                event.currentTarget.classList.add('wrong');
                document.getElementById('methodFeedback').innerHTML = 
                    `<span style="color: #ff4444;">❌ Errado! -20 HP<br>${method} não é adequado para esta operação!</span>`;
                takeDamage(20);
                gameState.phaseAttempts[4]--;
                updateAttemptsDisplay(4);
            }
        }

        function initDebuggingGame() {
            gameState.currentBug = 0;
            loadBug();
        }

        function loadBug() {
            if (gameState.currentBug >= gameState.bugs.length) {
                showReward('🐛', 'Caçador de Bugs!', 'Você corrigiu todos os erros HTTP!');
                addXP(250);
                setTimeout(() => completePhase(5), 2000);
                return;
            }

            const bug = gameState.bugs[gameState.currentBug];
            document.getElementById('bugDisplay').innerHTML = `
                <div style="color: #888;">// Erro #${gameState.currentBug + 1}:</div>
                <div>${bug.text}</div>
                <div style="color: #666; margin-top: 10px;">Servidor respondeu: ???</div>
            `;

            document.getElementById('statusFeedback').innerHTML = '';
            updateAttemptsDisplay(5);

            document.querySelectorAll('.status-card').forEach(card => {
                card.classList.remove('selected');
            });
        }

        function selectStatus(code, name) {
            if (!gameState.gameActive) return;

            const bug = gameState.bugs[gameState.currentBug];
            const cards = document.querySelectorAll('.status-card');

            cards.forEach(card => card.classList.remove('selected'));
            event.currentTarget.classList.add('selected');

            if (code === bug.code) {
                document.getElementById('statusFeedback').innerHTML = 
                    `<span style="color: #00ff88;">✅ Correto! Status ${code} - ${name}!</span>`;

                createParticles(window.innerWidth/2, window.innerHeight/2, '🐛');

                setTimeout(() => {
                    gameState.currentBug++;
                    loadBug();
                }, 1500);
            } else {
                document.getElementById('statusFeedback').innerHTML = 
                    `<span style="color: #ff4444;">❌ Errado! -25 HP<br>${code} significa "${name}", não se aplica aqui!</span>`;
                takeDamage(25);
                gameState.phaseAttempts[5]--;
                updateAttemptsDisplay(5);
            }
        }

        function buildParams() {
            if (!gameState.gameActive) return;

            const key1 = document.getElementById('paramKey1').value.trim();
            const value1 = document.getElementById('paramValue1').value.trim();
            const key2 = document.getElementById('paramKey2').value.trim();
            const value2 = document.getElementById('paramValue2').value.trim();

            const urlPreview = document.getElementById('urlPreview');

            const isCorrect = (key1 === 'nome' && value1 === 'Zé' && key2 === 'idade' && value2 === '25') ||
                            (key1 === 'idade' && value1 === '25' && key2 === 'nome' && value2 === 'Zé');

            if (isCorrect) {
                const queryString = `?${key1}=${value1}&${key2}=${value2}`;
                urlPreview.innerHTML = `https://api.exemplo.com/cadastro<span style="color: var(--warning);">${queryString}</span>`;

                document.getElementById('paramFeedback').innerHTML = 
                    `<span style="color: #00ff88;">✅ Perfeito! Parâmetros enviados com sucesso!</span>`;

                showReward('⚙️', 'Mestre dos Parâmetros!', 'Você domina Query Strings!');
                addXP(300);
                setTimeout(() => completePhase(6), 2000);
            } else {
                gameState.phaseAttempts[6]--;
                updateAttemptsDisplay(6);

                document.getElementById('paramFeedback').innerHTML = 
                    `<span style="color: #ff4444;">❌ Parâmetros incorretos! -20 HP<br>Dica: nome=Zé e idade=25</span>`;

                takeDamage(20);

                if (gameState.phaseAttempts[6] <= 0) {
                    triggerGameOver();
                }
            }
        }

        function resetParams() {
            document.getElementById('paramKey1').value = '';
            document.getElementById('paramValue1').value = '';
            document.getElementById('paramKey2').value = '';
            document.getElementById('paramValue2').value = '';
            document.getElementById('urlPreview').innerHTML = 'https://api.exemplo.com/cadastro<span style="color: var(--warning);"></span>';
            document.getElementById('paramFeedback').innerHTML = '';
        }

        function showReward(emoji, title, text) {
            const popup = document.getElementById('rewardPopup');
            document.getElementById('rewardIcon').textContent = emoji;
            document.getElementById('rewardTitle').textContent = title;
            document.getElementById('rewardText').textContent = text;

            popup.classList.add('show');
            createConfetti();
        }

        function closeReward() {
            document.getElementById('rewardPopup').classList.remove('show');
        }

        function createConfetti() {
            const colors = ['#ff0000', '#00ff00', '#0000ff', '#ffff00', '#ff00ff', '#00ffff'];

            for (let i = 0; i < 50; i++) {
                const confetti = document.createElement('div');
                confetti.className = 'confetti';
                confetti.style.left = Math.random() * 100 + 'vw';
                confetti.style.backgroundColor = colors[Math.floor(Math.random() * colors.length)];
                confetti.style.animationDuration = (Math.random() * 3 + 2) + 's';
                document.body.appendChild(confetti);

                setTimeout(() => confetti.remove(), 5000);
            }
        }

        function addXP(amount) {
            gameState.xp += amount;

            const newLevel = Math.floor(gameState.xp / 300) + 1;
            if (newLevel > gameState.level) {
                gameState.level = newLevel;
                showReward('⭐', `LEVEL UP! Nível ${newLevel}`, 'Você ficou mais forte!');
            }

            updateHUD();
        }

        function updateHUD() {
            const hpPercent = (gameState.hp / gameState.maxHp * 100);
            document.getElementById('hpBar').style.width = hpPercent + '%';
            document.getElementById('hpText').textContent = `${gameState.hp}/${gameState.maxHp}`;
            document.getElementById('xpBar').style.width = ((gameState.xp % 300) / 300 * 100) + '%';
            document.getElementById('xpText').textContent = `Nível ${gameState.level}`;
            document.getElementById('badgeCount').textContent = gameState.badges.length;

            if (hpPercent <= 30) {
                document.getElementById('hpBar').style.background = 'linear-gradient(90deg, #ff0000, #ff4444)';
            } else {
                document.getElementById('hpBar').style.background = 'linear-gradient(90deg, #ff4444, #ff8844)';
            }
        }

        function completePhase(phase) {
            if (!gameState.completedPhases.includes(phase)) {
                gameState.completedPhases.push(phase);
            }

            document.getElementById(`phase${phase}Modal`).classList.remove('active');

            if (phase < 6) {
                setTimeout(() => startPhase(phase + 1), 500);
            } else {
                showReward('👑', 'GUARDIÃO DA WEB!', 'Você completou toda a jornada HTTP!');
                setTimeout(() => {
                    alert('🎉 PARABÉNS! Você se tornou um Mestre HTTP!

Zé e Severino estão seguros graças a você!');
                }, 1000);
            }
        }

        function skipPhase(phase) {
            if (confirm('Pular esta fase? Você não ganhará XP e perderá 10 HP!')) {
                takeDamage(10);
                document.getElementById(`phase${phase}Modal`).classList.remove('active');
                if (phase < 6) {
                    setTimeout(() => startPhase(phase + 1), 500);
                }
            }
        }

        const style = document.createElement('style');
        style.textContent = `
            @keyframes fadeOut {
                to { opacity: 0; visibility: hidden; }
            }
            @keyframes particleFloat {
                to {
                    transform: translateY(-100px) rotate(360deg);
                    opacity: 0;
                }
            }
        `;
        document.head.appendChild(style);
    </script>
</body>
</html>
